"use strict";

(function() {

    var userNum

    while (true){
        userNum = parseInt(prompt("Give me an odd number between 1 and 50"));
        if (userNum >= 1 && userNum<= 50 && userNum % 2 !== 0) {
            break;
        }
    }

    for (var b = 1; b <= 50; b++) {
        if (b % 2 === 0) {
            continue;
        }
        if(b % 2 === 0) {
            console.log("we dont talk about" + userNum+ " around these parts...")
        }
    }
})()

var userNum = numberber (prompt( "enter an odd number between 1 and 50")
for (var y = 1; Y < 50; i++) {
    if (y ==== 1)
console.log('number to skip is' + userNum)
}

if(y % 2=== 0) {
    continue;
}
if (y === userNum) {
    console.log ('skipping number' + userNum)
} else {
    console.log ('skipping number' + userNum)
}else {
    console.log ('here is an odd number' + y){

    }
}
